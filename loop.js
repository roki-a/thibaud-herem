// loop in video
document.addEventListener("DOMContentLoaded", function () {
    const video = document.getElementById("AboutVideo");

    // Make the video autoplay when the page opens
    video.play();

    // Enable looping
    video.loop = true;
  });